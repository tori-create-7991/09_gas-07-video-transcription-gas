FROM alpine:3.19

# rcloneとFFmpegをインストール
RUN apk add --no-cache rclone ffmpeg bash

# 作業ディレクトリ
WORKDIR /app

# スクリプトをコピー
COPY rclone_converter.sh /app/
RUN chmod +x /app/rclone_converter.sh

# rclone設定用ボリューム
VOLUME ["/root/.config/rclone"]

ENTRYPOINT ["/app/rclone_converter.sh"]
